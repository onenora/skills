# Minimal skill repo: Xiangse booksource (JSON/XBS)

- `SKILL.md` — skill definition (Codex/pi/Claude compatible frontmatter)
- `scripts/xbs.js` — zero-dependency Node XBS <-> JSON CLI (XXTEA, built-in fs/crypto only)

Usage:

```bash
node scripts/xbs.js selftest
```
